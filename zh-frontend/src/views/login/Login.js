import React from 'react';
import { LoginForm } from '../../components';
import './Login.css';

export const Login = () => {

    return (
        <div className="login">
            <LoginForm />
        </div>
    );
}